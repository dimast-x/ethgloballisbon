interface:
  display_name: "Yareon Agent"
  short_description: "Operate policy-controlled Yareon procurement"
  default_prompt: "Use $yareon-agent to check readiness, inspect eligible offers, and place one explicitly authorized Yareon order."
